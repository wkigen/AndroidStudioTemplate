package ${rootPackage}.viewmodel;

import android.os.Bundle;
import ${rootPackage}.${viewPath}.${viewName+viewType};
import ${baseLibPath}.mvvm.AbstractViewModel;
import ${baseLibPath}.mvvm.AbstractViewModel;

/************************************************************
 * Author:  ${userName}
 * Description:    
 * Date: 
 ************************************************************/
public class ${viewName+VM} extends AbstractViewModel<${viewName+viewType}>{

}
